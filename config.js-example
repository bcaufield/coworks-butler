module.exports = {
  nexudus: {
    base: 'spaces.nexudus.com', // base nexudus url
    auth: 'user:pass' // user:pass format
  },
  server: {
    port: 4000
  },
  logs: {
    http: {
      path: __dirname + '/logs/http.log'
    },
    access: {
      path: __dirname + '/logs/access.log',
      maxSize: 52428800, // 50MB
      maxFiles: 5
    }
  }
};